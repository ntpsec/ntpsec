# This fragment will not work unmodified.  It will if you replace 'distro'
# with one of 'ubuntu' or 'debian', though. Distributioon packagers should
# feel free to modify this to plug in their pool addresses.

pool 0.distro.pool.ntp.org iburst
pool 1.distro.pool.ntp.org iburst
pool 2.distro.pool.ntp.org iburst
pool 3.distro.pool.ntp.org iburst

# The following sets edit modes for GNU EMACS
# Local Variables:
# mode:conf
# End:
