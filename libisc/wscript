
def build(ctx):

    libisc_source = [
    ]

    ctx(
        features="c bld_include src_include",
        source=libisc_source,
        target="libisc_obj",
    )

    ctx(
        target="isc",
        features="c cstlib bld_include src_include",
        use="libisc_obj"
    )
